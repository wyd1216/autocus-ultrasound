# Expected Demo Output

Running the demo command writes `pipeline_result.json`, `predictions.csv`, `metrics.json`, and stage images. Numeric values can vary slightly by dependency versions because the toy pipeline uses image statistics.
